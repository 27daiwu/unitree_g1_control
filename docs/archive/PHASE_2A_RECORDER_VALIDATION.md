# Phase 2A Recorder Validation

The original 10-second capture is `data/state/raw/g1_test_001.npz`. It contains complete 29DoF state, dual timestamps, IMU, tick, and metadata. The independent verifier must be run after any workspace migration.
